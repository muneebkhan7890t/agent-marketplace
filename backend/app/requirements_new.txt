# ─── pip install requirements ──────────────────────────────────────────
# Run: pip install -r requirements_new.txt

# requirements_new.txt contents:
# stripe
# razorpay
# mailchimp-marketing
# google-auth
# google-auth-oauthlib
# google-api-python-client
# requests

# Optional -- improves Knowledge Base (RAG) retrieval quality.
# Without this installed, app/ai/embeddings.py automatically falls back to a
# dependency-free hashing-based embedder (works, but pure keyword-level
# matching). Install this for real semantic search:
# sentence-transformers

# Optional -- error tracking (app/error_tracking.py). Without this installed,
# exceptions still get logged to console via capture_exception(), they just
# won't show up remotely in Sentry.
# sentry-sdk[fastapi]

# Note: transactional email (app/integrations/sendgrid/service.py) uses
# plain `requests` against SendGrid's REST API directly -- no separate
# sendgrid SDK package needed, it's already in the list above.